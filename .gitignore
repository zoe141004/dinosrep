# Python virtual environment
venv/
/venv/
*.venv
env/
/env/
ENV/
/ENV/

# Python cache files
__pycache__/
*.py[cod]
*$py.class

# IDE/Editor specific files
.vscode/
.idea/
*.swp
*~

# Output files (thường không nên đưa lên repo trừ khi là ví dụ nhỏ)
output/
processed_video.mp4
*.zip
grouped_by_id/
reid_inference_output.jpg

# Data files (nếu lớn hoặc không muốn chia sẻ)
# data/my_large_video.mp4
# data/large_dataset/

# Model Weights (QUAN TRỌNG!)
# Thường thì file weights rất lớn và không nên đưa trực tiếp lên Git.
# Bỏ comment dòng dưới nếu bạn muốn Git bỏ qua toàn bộ thư mục weights.
# Người dùng sẽ phải tự tải weights về theo hướng dẫn trong README.md.
models/weights/

# Nếu bạn muốn dùng Git LFS cho weights (yêu cầu cài đặt và cấu hình Git LFS):
# *.pth filter=lfs diff=lfs merge=lfs -text
# *.pt filter=lfs diff=lfs merge=lfs -text
# *.onnx filter=lfs diff=lfs merge=lfs -text

# Log files
*.log

# OS generated files
.DS_Store
Thumbs.db